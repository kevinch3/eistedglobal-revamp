PORT=3000
JWT_SECRET=your_long_random_secret_here
JWT_EXPIRES_IN=8h
DB_PATH=./data/eistedglobal.db
# URL of your Angular frontend — required in production so CORS allows requests
CORS_ORIGIN=http://localhost:4200
