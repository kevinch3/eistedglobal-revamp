export const environment = {
  production: false,
  // Dev targets the LOCAL API (bythfod sandbox data). The production build
  // (environment.prod.ts) is untouched and still points at the live server.
  apiUrl: 'http://localhost:3000/api',
  apiKey: 'n1qKmfBVmLeJzLnTwI6BxC5wYr2xpySp'
};
